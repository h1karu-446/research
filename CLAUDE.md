# 卒業研究プロジェクト: エリプソメトリー顕微鏡のDOF拡張

位相マスクと深層学習を用いて、エリプソメトリー顕微鏡の焦点深度(DOF)を拡張する卒業研究のコードリポジトリ。Claude Codeでこのリポジトリを開いたら、このファイルを前提知識として使うこと。

## 研究概要

- **課題**: エリプソメトリー測定は原理上70〜80°の斜め照明が必須(ブリュースター角近傍で偏光変化が最大化されるため)。斜め照明により光学的DOFが約1μmまで狭くなるが、実試料の表面凹凸は200μmレンジが普通で、大部分にピントが合わない。
- **アプローチ**: 位相マスク(PSFのz不変化)+ デコンボリューションによる計算イメージング(古典手法、卒論コア) → 将来的にはEnd-to-end深層学習でマスク設計と復元を同時最適化(修論方向)。
- **研究の4本柱**: 柱A(光学・エリプソメトリー原理=目的) → 柱B(PSF・DOFの物理=課題) → 柱C(計算イメージング/古典的解法=本研究のコア) → 柱D(深層学習=発展)。
- 詳細な背景・数式・文献リストは `docs/research-background.md` を参照すること。

## 実装マイルストーン(段階的プロトタイピング。各バージョンはディレクトリを分けて過去の実験を残す)

| Ver | 状態 | 内容 |
|---|---|---|
| v0.1 | 完了 | マスクなし標準PSFシミュレーション。`experiments/v0_1/v0_1_standard_psf.py` |
| v0.2 | 完了 | 立方位相マスク(CPM, φ(x,y)=α(x³+y³))を追加し、PSFのz軸不変性を可視化。`experiments/v0_2/v0_2_cubic_phase_mask.py` |
| v0.2b | 完了 | v0.2にエイリアシング対策(グリッド解像度アップ+ガードバンド)を適用。`experiments/v0_2/v0_2_cubic_phase_mask_aliasing_fixed.py` |
| v0.3 | 完了 | v0.2bのPSFを用いてWienerフィルタでデコンボリューション、EDOF実証(report_chang再現 = 卒論コア達成ライン)。`experiments/v0_3/v0_3_wiener_deconvolution.py` |
| v0.4 | 未着手 | 斜め照明70-80°への拡張(卒論完成ライン、★研究の独自性) |
| v0.5 | 未着手 | 試料凹凸200μmレンジでの性能評価(ストレッチゴール) |
| v0.6+ | 未着手 | 偏光保存マスク・深層学習導入(修論への布石) |

## ディレクトリ構成

- `experiments/v0_X/`: 各バージョンのコードと `outputs/`(図・npy)。バージョンごとにディレクトリを分離し、過去の実験コード・出力は上書きせず残す。
- `experiments/venv/`: 共有のPython仮想環境(Python 3.13, NumPy/SciPy/Matplotlib)。全バージョンで共通利用、構成は変更しない。
- `references/`: 参考論文PDF(report_chang.pdf, Cossairt関連, DeepDOF論文, end-to-end-cam.pdf, links.docxなど)
- `papers/`: 執筆中の卒論
- `results/`: 実験結果・出力のまとめ

## コーディング規約

- 言語/環境: Python + NumPy/SciPy(FFTベース)+ Matplotlib。GPUが必要になったらCuPyかPyTorchへ移行。
- 参照している論文のEq.番号にコード内コメントで対応させる(例: `Φ_DF(x1,y1;z) = kWm*r^2 ... (5)`)。v0.1のコードがこのスタイルの実例。
- 各バージョン完成時に「動くコード + 比較図(最低1枚) + 数値指標(PSNR等)」をセットで `outputs/` に残す。
- 新バージョンは既存バージョンのファイルを上書きせず、新規ディレクトリ(`experiments/v0_X/`)に追加する。

## 直近のタスク(v0.4)

v0.3までで「マスクなしPSF → CPM追加 → エイリアシング修正 → Wienerデコンボリューションによる EDOF実証」という卒論コアラインが一通り完成した。次は斜め照明70-80°への拡張(卒論完成ライン、★研究の独自性)。

v0.3までのレビューで残っている懸念点(v0.4着手前に一考の余地あり):
1. `PUPIL_HALF_WIDTH`(=71/25≈2.84)の物理的根拠(NA・波長・センサピクセルサイズとの関係)がDeepDOF本家コードにも明示されていない。v0.1〜v0.3は踏襲でよいが、v0.4で物理単位(実際のμm/角度)を扱う際は独自導出が必要。
2. v0.3のWienerデコンボリューションはPSNRが非単調(合焦近傍でCPMが優位、中程度のデフォーカスで逆転)。固定カーネルWienerデコンボリューション特有の効果と考えられるが、v0.4以降で斜め照明・非対称PSFを扱う際に再検証の余地あり(`experiments/v0_3/v0_3_wiener_deconvolution.py` のコメント参照)。
